max_epochs: 2
batch_size: 2
num_workers: 0
accelerator: auto
devices: 1
# Limit the batches for testing purposes. Remove these lines for full training.
limit_train_batches: 20
limit_val_batches: 5
log_every_n_steps: 1
gradient_clip_val: 1.0
